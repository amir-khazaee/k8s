apiVersion: v1
kind: Service
metadata:
  name: yarp-backend
  namespace: backend
  labels:
    app: yarp-backend
spec:
  type: LoadBalancer
  selector:
    app: yarp-backend
  ports:
    - name: http
      port: 8080        
      targetPort: 8080
      protocol: TCP
